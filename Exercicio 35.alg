algoritmo "semnome"

var
   C, N, R: Inteiro
inicio
      C <- 1
      Escreva ("Quer ver qua a tabuada? ")
      Leia (N)
      Repita
      R <- N * C
      Escreval (N, " x ", C, " = ", R)
      c <- C+1
      Ate(C > 10)
fimalgoritmo
