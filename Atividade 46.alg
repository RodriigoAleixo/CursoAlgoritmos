algoritmo "semnome"
// 46) Crie um programa que calcule e mostre na tela o resultado da soma entre 6 +
// 8 + 10 + 12 + 14 + ... + 98 + 100.
var
   C, S: Inteiro
inicio
      C <- 6
      S <- 0
      Enquanto (C <101) faca
      Escreva (C, " + ")
      S <- S + C
      C <- C + 2
      Fimenquanto
      Escreval (s)

fimalgoritmo
