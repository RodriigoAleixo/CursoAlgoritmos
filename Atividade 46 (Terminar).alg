algoritmo "semnome"
// 46) Crie um programa que calcule e mostre na tela o resultado da soma entre 6 +
// 8 + 10 + 12 + 14 + ... + 98 + 100.
var
   C, S: Inteiro
inicio
      C <- 6
      S <- 0
      Enquanto (C <= 100) faca
      Escreva (C, " + ")
      C <- C + 2
      S <- S + C
      Fimenquanto
      Escreval (s)

fimalgoritmo
